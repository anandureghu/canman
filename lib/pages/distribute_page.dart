import 'package:flutter/material.dart';

class DistributePage extends StatelessWidget {
  const DistributePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Distribute Page Content'));
  }
}
